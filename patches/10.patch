diff --git a/app/security/login.py b/app/security/login.py
new file mode 100644
index 0000000000000000000000000000000000000000..79f1c44e7802108dc845ca9ef6f0ff00bddb59b4
--- /dev/null
+++ b/app/security/login.py
@@ -0,0 +1,42 @@
+"""Small Tkinter login dialog backed by the independent authentication service."""
+
+import tkinter as tk
+from tkinter import messagebox, ttk
+
+from app.security.auth import AuthenticationError, AuthService
+
+
+def main() -> None:
+    auth = AuthService()
+    root = tk.Tk()
+    root.title("Matterport Ops — Sign in")
+    root.resizable(False, False)
+    frame = ttk.Frame(root, padding=20)
+    frame.grid()
+    ttk.Label(frame, text="Username").grid(row=0, column=0, sticky="w")
+    username = ttk.Entry(frame, width=32)
+    username.grid(row=1, column=0, pady=(2, 12))
+    ttk.Label(frame, text="Password").grid(row=2, column=0, sticky="w")
+    password = ttk.Entry(frame, width=32, show="•")
+    password.grid(row=3, column=0, pady=(2, 16))
+
+    def submit() -> None:
+        try:
+            session = auth.authenticate(username.get(), password.get())
+        except AuthenticationError as exc:
+            messagebox.showerror("Sign in failed", str(exc), parent=root)
+            password.delete(0, tk.END)
+            return
+        session.apply_to_environment()
+        messagebox.showinfo("Matterport Ops", f"Welcome, {session.username}.", parent=root)
+        root.destroy()
+
+    ttk.Button(frame, text="Sign in", command=submit).grid(row=4, column=0, sticky="e")
+    root.bind("<Return>", lambda _event: submit())
+    username.focus_set()
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()
+
